<div id="csricodexmlab" class="container-fluid bg-3 text-center jumbotron">
  <h1 class="margin">Users</h1>
</div>
<p>
  <a style="color:white" class="btn btn-success btn-lg" href="/users/add">
    <span class="glyphicon glyphicon-plus"></span> Add new user
  </a>
</p>

<div class="row">
  <div class="col-md-11">
    <div class="table-responsive">
    <table class="table table-hover">
      <tr id="Heading">
        <th>Username</th>
        <th>Email</td>
        <th placeholder="user/ admin">Role</td>
        <th></th>
        <th></th>
      </tr>
      {{#users}}
      <tr>
        <td class="info"><h4>{{Username}}</h4></td>
        <td class="info"><h4>{{Email}}</h4></td>
        <td><h4>{{is_admin}}</h4></td>
        <td><h4><a href="/users/edit/{{user_id}}">Edit</a></h4></td>
        <td><h4><a href="/users/delete/{{user_id}}">Delete</a></h4></td>
      </tr>
      {{/users}}
    </table>
  </div>
  </div>
</div>
